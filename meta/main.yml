---
dependencies:
  - role: getvalkyrie.aegir
    src: git+http://github.com/GetValkyrie/ansible-role-aegir.git
    aegir_makefile: "{{ valkyrie_makefile }}"
    aegir_frontend_url: "{{ valkyrie_frontend_url }}"
  - role: getvalkyrie.skynet
    src: git+http://github.com/GetValkyrie/ansible-role-skynet.git
    skynet_enable: "{{ valkyrie_use_skynet }}"
    skynet_repo: "{{ valkyrie_skynet_repo }}"
    skynet_version: "{{ valkyrie_skynet_version }}"
    skynet_update: "{{ valkyrie_skynet_update }}"

galaxy_info:
  author: ergonlogic
  description: Valkyrie - an opinionated local dev stack that makes features/git based Drupal development easy.
  company: "GetValyrie"
  license: "license (BSD, MIT)"
  min_ansible_version: 1.4
  platforms:
  - name: EL
    versions:
    - all
  - name: GenericUNIX
    versions:
    - all
  - name: Fedora
    versions:
    - all
  - name: opensuse
    versions:
    - all
  - name: GenericBSD
    versions:
    - all
  - name: FreeBSD
    versions:
    - all
  - name: Ubuntu
    versions:
    - all
  - name: SLES
    versions:
    - all
  - name: GenericLinux
    versions:
    - all
  - name: Debian
    versions:
    - all
  categories:
    - development
    - web
